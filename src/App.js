import React from 'react';
import { App as AppCSS } from './AppStyles';

function App({ env }) {
	return (
		<>
			<header>

			</header>
			<main>

			</main>
			<footer>
			
			</footer>
		</>
	);
}

export default App;
